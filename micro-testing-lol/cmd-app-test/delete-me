 App app
    char* name
    char* version
    Commands Array (dynArray)
      int size
      int capacity
      void* data
        command
            B Flags Array (dynArray)
                int size
                int capacity
                void* data
                    bFlag
                        char* name
            S Flags Array (dynArray)
                int size
                int capacity
                void* data
                    sFlag
                        char* name
                        char* value
        command
            B Flags Array (dynArray)
                int size
                int capacity
                void* data
                    bFlag
                        char* name
            S Flags Array (dynArray)
                int size
                int capacity
                void* data
                    sFlag
                        char* name
                        char* value
